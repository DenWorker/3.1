function showEditModal(userId) {
    let editModal = new bootstrap.Modal(document.getElementById('editModal'));

    editModal.show();
}

function showDeleteModal(userId) {
   let editModal = new bootstrap.Modal(document.getElementById('deleteModal'));
   editModal.show();
}